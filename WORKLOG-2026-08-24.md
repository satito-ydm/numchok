# บันทึกงาน — ปล่อยปลานำโชค

**วันที่** 24 สิงหาคม 2569
**Repo** https://github.com/satito-ydm/numchok (public)
**พรีวิว** https://satito-ydm.github.io/numchok/
**ไฟล์หลัก** `index.html` — single-file (HTML + CSS + JS ในไฟล์เดียว, ไม่มี build step)

---

## สรุปสั้น

เว็บเดิมเป็นโทนฟ้า-น้ำเงินสดใส วันนี้เปลี่ยนเป็นโทน **กรมท่า-ทอง** ที่สกัดค่าสีจากไฟล์โลโก้จริง
พร้อมเพิ่มลูกเล่น 3 อย่าง (ปลาว่ายตามสกรอลล์ / แท็บเสริมดวง / กดดูรูปใหญ่) ลดขนาดรูปลง 61%
แก้บั๊กที่มีอยู่เดิม 5 จุด และขึ้น GitHub + GitHub Pages เรียบร้อย

| commit | เรื่อง |
|---|---|
| `f765948` | เปลี่ยนโทนสี + ปลาว่ายตามสกรอลล์ + แท็บเสริมดวง |
| `5fb3456` | แก้ favicon เป็น relative path ให้ทำงานใต้ subpath ของ Pages |
| `c5a3cf7` | Lightbox กดดูรูปใหญ่ในแกลเลอรี |
| `fb217f3` | เปลี่ยนรูป hero + รูปเดิมเป็นพื้นหลังจาง + ตั้ง GA4 |

---

## 1. โทนสีใหม่ (สกัดจากโลโก้จริง)

ไม่ได้กะสีเอง — อ่านค่าพิกเซลจากไฟล์โลโก้ที่ได้รับมาโดยตรง

| บทบาท | ค่าสี | ใช้ที่ไหน |
|---|---|---|
| กรมท่าเข้มสุด | `#00102D` | พื้นโลโก้, footer, section วันเกิด, CTA |
| น้ำเงินกลางคืน | `#062A45` | หัวข้อ, สีแบรนด์รอง |
| น้ำเงินเจ้าพระยา | `#0E4C79` | ลิงก์, ตัวปลาในไอคอน |
| ทองดอกดาวเรือง | `#D9A93E` | **สีนำของแบรนด์** — ปุ่มหลัก, ตัวเลข, เส้นคั่น |
| ทองไฮไลต์ / ทองเข้ม | `#F8D77C` / `#BD7D2F` | ไล่เฉดในปุ่มและตัวอักษร |
| ทองสำหรับพื้นสว่าง | `#9C641F` | ทองที่ยังอ่านออกบนพื้นขาว (AA) |
| ฟ้าหมอก | `#5FA8C7` | สีเสริม |

**ทุกคู่สีผ่าน WCAG AA แล้ว** จุดที่ต้องระวังและได้จัดการไป:

- ทอง `#D9A93E` บนพื้นขาวได้แค่ **2.17:1** → **ห้ามใช้ทองเป็นตัวอักษรบนพื้นสว่าง** จึงเพิ่ม `--gold-ink #9C641F` (4.93:1) ไว้ใช้แทน
- `--muted` เดิม `#7C8C97` ได้ 3.47:1 (ตก) → เปลี่ยนเป็น `#5F6E79` (5.26:1)
- eyebrow เดิมได้ 4.49:1 (ตกเส้นยาศ) → เปลี่ยนเป็น `#26708F` (5.53:1)

วิธีทำ: เปลี่ยนค่าใน `:root` แล้ว map ชื่อตัวแปรเดิม (`--blue-800` ฯลฯ) มาที่โทนใหม่
จึงไม่ต้องรื้อ CSS ทั้งไฟล์ แล้วตามแก้สีที่ hardcode ไว้อีกราว 15 จุด

**สีที่ตั้งใจไม่แตะ:** เขียว LINE `#06C755`, น้ำเงิน Facebook `#1877F2`, และสีประจำวันทั้ง 7 ในการ์ดวันเกิด (เป็นสีตามความเชื่อ ห้ามเปลี่ยน)

---

## 2. โลโก้

ได้รับไฟล์โลโก้ 2 เวอร์ชัน แยกใช้ตามพื้นหลัง

| ไฟล์ | ใช้ที่ | หมายเหตุ |
|---|---|---|
| `images/logo-mark-light.png/.webp` | **header** | ปลากรมท่า+ซุ้มทอง สำหรับพื้นสว่าง |
| `images/logo-mark.png/.webp` | สำรอง | ปลาขาว+ทอง สำหรับพื้นเข้ม |
| `images/logo-lockup.png/.webp` | **footer** | โลโก้เต็ม + สโลแกน |
| `favicon.ico`, `favicon-16/32`, `apple-touch-icon`, `icon-512` | ทั้งเว็บ | สร้างจากตราสัญลักษณ์ |

**เทคนิคการแยกพื้นหลัง:** ไม่ได้ใช้การตัดแบบ threshold (ซึ่งจะเหลือขอบหยัก) แต่ใช้วิธี
unpremultiply — คำนวณ alpha จากส่วนต่างของสีกับพื้น แล้วถอดสีกลับ ได้ขอบเนียนไม่มีฝ้าขาว
ทดสอบวางบนพื้นขาว/เทา/กรมท่าแล้วไม่มีขอบเห็น

**สโลแกนจากโลโก้** "ส่งต่อชีวิต ส่งต่อบุญ ส่งต่อความสุข" ถูกนำมาใช้ใน hero และใต้ชื่อแบรนด์ใน header

---

## 3. ลูกเล่นที่เพิ่ม

### 3.1 ขั้นตอน 1-4 — ปลาว่ายตามสกรอลล์

- เส้นโค้งคดเคี้ยว **คำนวณจากตำแหน่งจริง** ของวงกลมทั้ง 4 (Catmull-Rom → เบซิเยร์) ไม่ได้ hard-code
  จึงยืดหยุ่นตามความกว้างจอเองอัตโนมัติ
- section ถูก **pin** ไว้ (sticky, ระยะสกรอลล์ 300vh) การสกรอลล์ขับตำแหน่งปลา
- ปลาถึงขั้นที่ 4 ที่ **90%** ของระยะ แล้วค้างไว้ก่อนปล่อยให้เลื่อนไป section ถัดไป
- เส้นทองไล่ระบายตามปลา วงกลมที่ผ่านแล้วเปลี่ยนเป็นทอง
- ปลาว่าย **เหนือเส้น 52px** และอยู่คนละ SVG layer เพื่อไม่ให้บังตัวเลขและไม่ถูกวงกลมบัง
- **จอ ≤1024px หรือผู้ใช้เปิด reduced-motion → ปิดทั้งหมด** แสดงทุกขั้นเป็นสถานะพร้อม

### 3.2 แท็บเสริมดวง

แทนที่ section วันเกิดเดิม เป็น 2 แท็บ (รองรับคีย์บอร์ดตามมาตรฐาน ARIA: ← → Home End)

- **แท็บ 1 `set วันเกิด`** — การ์ด 8 วัน + บรรทัด "คละ:" ตัวอย่างปลาแต่ละวัน
- **แท็บ 2 `set ปล่อยปลาแนะนำ`** — 3 การ์ด (เงิน/ความรัก/โรคภัย) พร้อมรูปปลา จำนวน และความหมาย

### 3.3 Lightbox ในแกลเลอรี

- ทุกรูปเป็น `<button>` จริง จึงแท็บถึงและกด Enter ได้
- Esc / ปุ่ม ✕ / กดพื้นหลัง = ปิด · ลูกศร ← → และปุ่มบนจอ = เลื่อน (วนรอบ)
- ล็อก scroll ตอนเปิด, วนโฟกัสอยู่ในกล่อง, ปิดแล้วโฟกัสกลับที่รูปเดิม
- **จำกัดขนาดไม่เกิน 1.9 เท่าของความละเอียดจริง** เพราะรูป 6 จาก 8 มีต้นฉบับแค่ 600×400
  ถ้าปล่อยเต็มจอจะเบลอชัดเจน

### 3.4 โมชันอื่น ๆ

แถบทองบอกความคืบหน้าการเลื่อนหน้า · น้ำไหลเบา ๆ ใน hero · รูปและป้ายลอยขยับแบบลอยน้ำ ·
ประกายทองวิ่งผ่านปุ่มโทรตอน hover · ขีดทองใต้เมนู · ปลาเอียงตอน hover · ขอบทองในแกลเลอรี

ใช้ CSS property `translate`/`rotate` แทน `transform` จึงไม่ชนกับ transform เดิมที่มีอยู่
และ `prefers-reduced-motion` ครอบคลุมของใหม่ทั้งหมด

---

## 4. รูป Hero

- เปลี่ยนเป็นรูป**พระสงฆ์สวดอธิษฐานจิตหน้ากะละมังปลาที่ท่าน้ำ**
- **ครอปลายน้ำกล้อง Samsung ออก** (เดิมพาดมุมซ้ายล่างพร้อมชื่อคนถ่าย ดูไม่โปร)
  ครอปเป็น 1108×942 (อัตราส่วน 4:3.4) ให้เห็นทั้งพระและแถวกะละมัง
- **รูปแม่น้ำเดิมย้ายไปเป็นพื้นหลังจาง ๆ** ของทั้ง section hero (opacity 20%, blend luminosity)
- ตรวจ contrast จากภาพที่เรนเดอร์จริง (353,992 พิกเซล): พื้นหลังสว่างสุด `#224860`
  → ตัวขาว **9.70:1**, ตัวทอง **5.92:1** — ผ่าน AA สบาย
- `og:image` / `twitter:image` ชี้มารูปใหม่ และแก้ขนาดที่ประกาศไว้ผิด (เดิมบอก 1780×1335 แต่ไฟล์จริง 1500×1126)

---

## 5. ความเร็ว

| รายการ | ผล |
|---|---|
| แปลง WebP ครบ 27 รูป | **เล็กลง 61%** (3,491 → 1,366 KB) |
| hero มี 2 ขนาด (760w / 1108w) | จอ desktop ปกติโหลดตัวเล็ก ไม่ลากไฟล์ใหญ่มาย่อ |
| ฟอนต์ตัดจาก 6 น้ำหนักเหลือ 4 | น้ำหนัก 300 กับ 500 ไม่ถูกใช้ที่ไหนเลยในเว็บ |
| `<picture>` + `<source>` ทุกภาพ | มี `.jpg/.png` เป็น fallback ครบ |

ยืนยันแล้วว่าเบราว์เซอร์เสิร์ฟ webp จริงทั้ง 27 รูป และไม่มีรูปไหนโหลดพัง

---

## 6. บั๊กที่เจอและแก้ (ทั้งหมดเป็นของเดิมที่มีอยู่ก่อน)

1. **`.nb` ถูกใช้ใน `<h1>` แต่ไม่เคยถูกประกาศใน CSS**
   → "ปลาหน้าเขียง" ถูกตัดคำกลางวลีเป็น "ปลาหน้า / เขียง" ทุกครั้ง

2. **Google Ads ยิง request จริงด้วย ID ปลอมทุกครั้งที่เปิดหน้า**
   → เขียน guard ใหม่ ถ้ายังเป็น `XXXXXXXXXX` จะไม่โหลดสคริปต์เลย

3. **`margin-top` ของข้อความใต้ปุ่ม CTA ไม่เคยทำงาน**
   → `.cta-note` เป็น `<p>` ใน `.cta` เลยโดน `.cta p{margin:0 auto 30px}` ทับด้วย specificity
   ค่าที่เบราว์เซอร์ใช้จริงคือ `0px` มาตลอด แก้เป็น `.cta p.cta-note`

4. **`<source>` สร้างกล่อง layout** (บั๊กที่ผมทำเกิดเองตอนใส่ WebP)
   → `<source>` **ไม่ได้**เป็น `display:none` โดยปริยาย พอ parent เป็น `display:contents`
   มันถูกยกขึ้นมาเป็น grid item จริง ทำให้การ์ดกลายเป็น 4 ช่องแทน 3
   **กระทบทุก `<picture>` ในหน้า** แก้ด้วย `source{display:none}`

5. **แถบเมนูล้นและตัดปุ่ม LINE ในช่วงจอ 861–1100px**
   → วัดได้ว่าต้องการ 1011px แต่มีที่ 945px ซ่อนปุ่ม LINE ในเมนูต่ำกว่า 1100px
   (ยังกดได้จากปุ่มลอยและปุ่มใน hero)

---

## 7. Git / Deploy

- โปรเจกต์นี้อยู่ใต้ git repo ของ **home directory** (`/Users/satit.o`) ที่ไม่มี `.gitignore`
  → **ห้าม `git add .` ตรงนั้นเด็ดขาด** จะกวาด `.claude.json`, `.bash_history`, Creative Cloud ขึ้นไปด้วย
  จึง `git init` แยกให้โปรเจกต์นี้เอง
- push แรกพังด้วย `RPC failed; HTTP 400` — **ไม่ใช่ปัญหา auth** แต่เป็นขนาด payload 12.26 MiB
  แก้ด้วย `git config http.postBuffer 524288000`
- `.gitignore`: `.DS_Store`, `.omc/`, `*.log`
- GitHub Pages เปิดแล้วที่ `main` / root → https://satito-ydm.github.io/numchok/
- แก้ favicon จาก `/favicon.ico` เป็น `favicon.ico` เพราะ absolute path จะพังใต้ subpath

---

## 8. Analytics

- **GA4 `G-TGXL827197`** — ตัวเดียวกับที่ `poiplanamchok.com` ใช้อยู่ ตั้งแล้วและยืนยันว่ายิงจริง
- Google Ads (`AW-`) และ conversion label ยังเป็น placeholder เพราะ **เว็บเดิมไม่มี Google Ads ติดอยู่เลย**
- แก้ค่าได้ที่ `window.TRACKING` ใน `<head>` จุดเดียว 4 บรรทัด

---

## ⚠️ ค้างอยู่ — ต้องการการตัดสินใจ

### ต้องรีวิวเนื้อหาก่อนใช้จริง
เนื้อหา **"คละปลาแต่ละวัน" (8 วัน)** และ **"set แนะนำ" (3 ชุด)** เป็นข้อเสนอที่ผมประกอบขึ้น
จากความหมายปลาที่มีอยู่ในเว็บเดิมเท่านั้น ไม่ได้แต่งความเชื่อใหม่ แต่เป็นคำแนะนำเชิงความเชื่อของร้าน
**ต้องให้เจ้าของร้านตรวจก่อน**

จุดที่อ่อนที่สุดคือ **set ความรัก** — รายการปลา 12 ชนิดของร้านไม่มีตัวไหนระบุเรื่องความรักตรง ๆ เลย
จึงประกอบจากความหมายข้างเคียง (ตะเพียน / หอยขม / ปลากราย)

### เรื่องโดเมน
`poiplanamchok.com` และ `www` ชี้ไป `150.95.23.169` และ**มีเว็บรันอยู่จริง (HTTP 200)**
ถ้าเปลี่ยน DNS ไป GitHub Pages เว็บเดิมจะถูกแทนที่ทันที — ยังไม่ได้ทำ รอการยืนยัน

ทางเลือกที่ปลอดภัยกว่า: ใช้ subdomain เช่น `new.poiplanamchok.com` (CNAME → `satito-ydm.github.io`)
ทดสอบให้พอใจก่อนค่อยสลับ

### เรื่อง GTM
เว็บเดิมมี container `GTM-MTZRB7ST` ด้วย ยังไม่ได้ใส่เพราะ **เสี่ยงนับ GA4 ซ้ำ 2 เท่า**
ถ้าใน container มีแท็ก GA4 ตัวเดียวกัน ต้องเลือกทางเดียว: ใส่ GA4 ตรง ๆ (ปัจจุบัน) หรือใส่ GTM แทน

### อื่น ๆ
- **FAQ ยังหายอยู่** — CSS (`.faq-item`) และ JS ยังอยู่ครบ ขาดแค่ HTML เป็นโอกาส SEO ที่ยังไม่ได้ใช้
- **ยังไม่มีเซคชันราคา** ตามที่สั่งไว้ ทั้งที่ปุ่มเขียนว่า "ทักไลน์สอบถามราคา"
- **บนมือถือมี CTA ซ้อนกัน 3 ชั้น** (แถบล่างติดหน้าจอ + ปุ่มลอย 2 วง + ปุ่มใน hero)
  ปุ่มลอย LINE ทับปุ่ม "ทักไลน์สอบถามราคา" พอดี — ของเดิมเป็นแบบนี้ ไม่ได้แตะเพราะเป็นการตัดสินใจเชิงธุรกิจ
- **รูปแกลเลอรีความละเอียดต่ำ** 6 จาก 8 รูปมีต้นฉบับแค่ 600×400 ถ้าอยากให้ดูรูปใหญ่ได้สวยต้องมีไฟล์ใหญ่กว่านี้
- **`canonical` และ `og:url` ยังชี้ `poiplanamchok.com`** ถูกแล้วถ้าใช้ Pages เป็นแค่ที่พรีวิว
  แต่ถ้าจะให้ Pages เป็นตัวจริงต้องแก้ ไม่งั้น Google จะไม่ index

---

## หมายเหตุสำหรับคนทำงานต่อ

- ไฟล์เดียวจบ ไม่มี build step — แก้ `index.html` แล้ว refresh ได้เลย
- ดูเว็บแบบ local: `cd <โฟลเดอร์> && python3 -m http.server 8899` แล้วเปิด `http://localhost:8899`
- ไฟล์ต้นฉบับความละเอียดสูงอยู่ใน `images/_original/` (ไม่ได้ถูกอ้างอิงจากหน้าเว็บ แต่เก็บไว้เป็น backup)
- โทนสีทั้งหมดคุมจาก `:root` จุดเดียว
- ถ้าจะเพิ่มรูปในแกลเลอรี ใส่เป็น `<figure><button class="gal-btn">…</button></figure>`
  แล้ว lightbox จะรับไปเองอัตโนมัติ
